{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Draft Paze UCP payment instrument",
  "description": "Single-use reference to a Paze JavaScript SDK result stored outside model context.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "session_id", "credential_reference"],
  "properties": {
    "type": {
      "type": "string",
      "const": "paze"
    },
    "session_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255,
      "description": "UCP checkout ID passed to the Paze SDK as sessionId."
    },
    "credential_reference": {
      "type": "string",
      "minLength": 16,
      "description": "Opaque, expiring, single-use reference resolved by deterministic platform code."
    },
    "payload_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 255
    },
    "card_network": {
      "type": "string",
      "enum": ["VISA", "MASTERCARD", "DISCOVER"]
    },
    "pan_last_four": {
      "type": "string",
      "pattern": "^[0-9]{4}$"
    }
  }
}
